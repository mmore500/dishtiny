TARGET_NAMES += cell
TARGET_NAMES += config
TARGET_NAMES += genome
TARGET_NAMES += operations
TARGET_NAMES += peripheral
# TARGET_NAMES += record TODO
TARGET_NAMES += runninglog
TARGET_NAMES += services
TARGET_NAMES += spec
TARGET_NAMES += utility
#TARGET_NAMES += viz TODO
#TARGET_NAMES += web TODO
TARGET_NAMES += world

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateForwarding

Contains code to run a simulation session with a `dish2::ThreadWorld` object.
Mediates data collection, inter-cpu synchronization (if configured), and simulation end conditions (i.e., updates or time elapsed).

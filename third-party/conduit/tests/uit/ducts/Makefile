TARGET_NAMES += ducts
TARGET_NAMES += intra
TARGET_NAMES += mock
TARGET_NAMES += proc
TARGET_NAMES += thread

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateForwarding

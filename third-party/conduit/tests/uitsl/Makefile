TARGET_NAMES += algorithm
TARGET_NAMES += chrono
TARGET_NAMES += concurrent
TARGET_NAMES += containers
TARGET_NAMES += countdown
TARGET_NAMES += datastructs
TARGET_NAMES += debug
TARGET_NAMES += distributed
TARGET_NAMES += fetch
TARGET_NAMES += math
TARGET_NAMES += meta
TARGET_NAMES += mpi
TARGET_NAMES += nonce
TARGET_NAMES += parallel
TARGET_NAMES += polyfill
TARGET_NAMES += utility

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateForwarding

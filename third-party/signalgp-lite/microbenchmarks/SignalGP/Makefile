TARGET_NAMES += arithmetic
TARGET_NAMES += complete
TARGET_NAMES += nop
TARGET_NAMES += sans_regulation

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/microbenchmarks/MaketemplateRunning

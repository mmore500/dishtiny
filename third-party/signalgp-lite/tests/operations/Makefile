TARGET_NAMES += actions
TARGET_NAMES += binary
TARGET_NAMES += comparison
TARGET_NAMES += flow_global
TARGET_NAMES += flow_local
TARGET_NAMES += unary

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateForwarding

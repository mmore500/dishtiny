TEST_NAMES += Anchor
TEST_NAMES += JumpIf
TEST_NAMES += JumpIfNot
TEST_NAMES += RegulatorAdj
TEST_NAMES += RegulatorGet
TEST_NAMES += RegulatorSet

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateRunning

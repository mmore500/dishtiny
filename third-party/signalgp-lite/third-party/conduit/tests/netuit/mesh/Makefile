TARGET_NAMES += Mesh
TARGET_NAMES += MeshNode
TARGET_NAMES += MeshNodeInput
TARGET_NAMES += MeshNodeOutput
TARGET_NAMES += MeshTopology

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateUniproc

TARGET_NAMES += mapping_utils
TARGET_NAMES += math_utils
TARGET_NAMES += ratio_to_double
TARGET_NAMES += shift_mod

TO_ROOT := $(shell git rev-parse --show-cdup)

include $(TO_ROOT)/tests/MaketemplateUniproc
